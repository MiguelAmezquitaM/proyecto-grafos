import java.awt.event.MouseListener;

import javax.swing.JPanel;

public class Paint extends JPanel implements MouseListener extends JPanel implements {
    

    public static void paintLine(Graphics g, int x1, int y1, int x2, int y2, int tam){
        int xaux = 0; int yAux=0;
        ((Graphics2D)g).setRenderingHint(RenderingHints.KEY_ANTIALIASING, RenderingHints)//El profe dejo un video?
    }
}